export const TEST_TIMEOUTS = {
	VISUAL: 15000,
	VISIBLE: 10000,
	MODAL_TRANSITION: 500,
} as const;

export const TEST_COUNTS = {
	DEFAULT_MOVIES: 5,
	MIN_MOVIES: 2,
} as const;

export const TEST_SCROLL = {
	THRESHOLD: 1000,
	BODY_HEIGHT: 2000,
} as const;
